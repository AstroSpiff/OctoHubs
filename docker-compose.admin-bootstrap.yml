services:
  app:
    environment:
      ADMIN_PASSWORD: ""
      ADMIN_PASSWORD_FILE: /run/secrets/octohubs_admin_password
    secrets:
      - octohubs_admin_password

secrets:
  octohubs_admin_password:
    file: ${ADMIN_PASSWORD_FILE:?Set ADMIN_PASSWORD_FILE to the host initial-admin password file}
