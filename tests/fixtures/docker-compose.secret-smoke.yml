services:
  postgres:
    container_name: ${OCTOHUBS_SECRET_SMOKE_CONTAINER:?Set the smoke container name}
    restart: "no"
    volumes:
      - octohubs-secret-smoke-data:/var/lib/postgresql/data

volumes:
  octohubs-secret-smoke-data:
